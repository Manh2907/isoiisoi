server:
  http_listen_port: 9080

clients:
  - url: http://vm1:3100/loki/api/v1/push # Thay bang IP VM1 của bạn

scrape_configs:
  - job_name: system
    static_configs:
    - targets: [localhost]
      labels:
        host: {{ inventory_hostname }}
        __path__: /var/log/*.log
